# jjcat v0.9.2

jjcat의 signed beta updater channel을 실제로 사용하는 첫 후속 버전이다. `v0.9.1`
사용자는 앱 안에서 이 버전을 발견하고, 다운로드·서명 검증·설치 후 직접 재시작할 수
있다.

## 지원 범위

- Apple Silicon Mac (`arm64`)
- macOS 13 이상
- Jujutsu `0.43.x`
- 로컬 저장소와 OpenSSH host alias 기반 원격 저장소

## v0.9.1에서 업데이트하기

1. 앱 메뉴의 **Check for Updates…**를 선택한다. 자동 확인에서 이미 발견했다면 이
   단계는 생략한다.
2. status bar 우측의 **Download jjcat 0.9.2**를 선택한다.
3. 다운로드와 signature 검증이 끝나 **Restart to update**가 나타날 때까지 기다린다.
4. 진행 중인 repository operation이 없는지 확인하고 **Restart to update**를 선택한다.

jjcat은 update를 자동으로 내려받거나 강제로 재시작하지 않는다. 설치된 `v0.9.1`에
내장된 public key와 updater archive의 Tauri Minisign signature가 일치해야 설치가
진행된다.

## 다운로드

수동 설치가 필요한 경우 다음 asset을 사용할 수 있다.

- `jjcat_0.9.2_aarch64.dmg`: 일반 설치용 디스크 이미지
- `jjcat_0.9.2_aarch64.app.zip`: 압축된 앱 번들
- `jjcat_0.9.2_aarch64.app.tar.gz`: 서명된 updater archive
- `jjcat_0.9.2_aarch64.app.tar.gz.sig`: updater archive의 Tauri Minisign signature
- `latest-beta.json`: beta updater manifest
- `SHA256SUMS`: 모든 release asset의 다운로드 체크섬

같은 디렉터리에서 다음 명령으로 다운로드를 검증할 수 있다.

```sh
shasum -a 256 -c SHA256SUMS
```

## 배포 파이프라인

- versioned updater assets의 검증과 게시가 끝난 뒤 rolling `updater-beta` manifest를
  교체한다.
- GitHub Release publisher는 Node 24 기반 `softprops/action-gh-release@v3`를 사용한다.

## 서명 및 Gatekeeper 경계

Updater archive는 Tauri Minisign signature를 필수로 검증한다. macOS app 자체는 번들
무결성을 위해 ad-hoc 서명했지만 Apple Developer ID로 서명하거나 Apple에
notarization하지 않았다. 수동 설치 시 Gatekeeper가 차단하면 Finder의 **열기** 또는
**시스템 설정 → 개인정보 보호 및 보안 → 확인 없이 열기**를 사용한다. Gatekeeper를
끄거나 quarantine 속성을 제거할 필요는 없다.

## 알려진 제한

- Intel Mac용 빌드는 제공하지 않는다.
- Developer ID 서명과 notarization을 제공하지 않는다.
- beta update는 자동 다운로드나 강제 재시작을 하지 않는다.
- 원격 저장소는 비대화형 OpenSSH 접속과 원격 `jj` 설치가 필요하다.

문제가 있으면 [GitHub Issues](https://github.com/zrma/jjcat/issues)에 재현 절차를
남겨 달라.
